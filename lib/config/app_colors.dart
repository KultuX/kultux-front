import 'package:flutter/material.dart';

class AppColors{
  AppColors._();

  // == Brand == //
  static const green = Color.fromARGB(255, 166, 226, 70);
  static const greenDark = Color.fromARGB(255, 99, 153, 34);

  static const pageBg = Color.fromARGB(255, 241, 239, 233);
  static const cardBg = Color.fromARGB(255, 248, 247, 244);
  static const white = Color.fromARGB(255, 255, 255, 255);

  // == Text == //
  static const text         = Color.fromARGB(255, 26,  26,  26);
  static const textSoft     = Color.fromARGB(255, 107, 107, 107);
  static const textHint     = Color.fromARGB(255, 176, 176, 176);

  // == Border == //
  static const border       = Color.fromARGB(255, 224, 221, 214);
  static const borderLight  = Color.fromARGB(255, 232, 232, 232);

  // == Semantic == //
  static const error        = Color.fromARGB(255, 198, 40,  40);
  static const errorBg      = Color.fromARGB(255, 255, 235, 238);
  static const success      = Color.fromARGB(255, 166, 226, 70);
  static const warning      = Color.fromARGB(255, 249, 168, 37);
  static const info         = Color.fromARGB(255, 107, 107, 107);

  // == Status badges == //
  static const statusSoon   = Color.fromARGB(255, 24,  95,  165);
  static const statusClosed = Color.fromARGB(163, 163, 45,  45);

  // == Navbar / Sidebar == //
  static const navBg        = Color.fromARGB(255, 0,   0,   0);

  // == Overlay / Shadow == //
  static const overlay      = Color.fromARGB(102, 0,   0,   0);
  static const shadow       = Color.fromARGB(16,  0,   0,   0);

  // == Action buttons (sobre imágenes) == //
  static const actionButtonBg     = Color.fromARGB(102, 0,   0,   0);   // fondo botón guardar/compartir
  static const actionButtonBgHover = Color.fromARGB(140, 0,   0,   0);  // hover

// == Image navigation arrows == //
  static const imageNavBg         = Color.fromARGB(140, 0,   0,   0);   // fondo flechas carrusel

// == Bookmark == //
  static const bookmarkActive     = AppColors.green;                     // guardado = verde
  static const bookmarkInactive   = AppColors.white;                     // no guardado = blanco

// == Image gradient overlays == //
  static const imageGradientTop    = Color.fromARGB(136, 0,   0,   0);  // degradado superior imagen
  static const imageGradientBottom = Color.fromARGB(204, 0,   0,   0);  // degradado inferior imagen

// == Category badge (sobre imagen) == //
  static const categoryBadgeBg    = Color.fromARGB(115, 0,   0,   0);  // fondo etiqueta categoría
  static const categoryBadgeBorder = Color.fromARGB(77,  166, 226, 70); // borde verde translúcido

// == Images Colors == //
  static const imagePlaceholder = Color.fromARGB(255, 212, 208, 200);
  static const imageErrorIcon = Color.fromARGB(255, 153, 153, 153);



  static const statusNext = Color.fromARGB(136, 166, 226, 70);
  static const statusCancel = Color.fromARGB(92, 255, 82, 100);


  static const dateGreenBg      = Color(0x4D639922);                  // Fondo verde de la fecha
  static const dateGreenText    = Color(0xFFC0DD97);                  // Texto e icono verde de la fecha
  static const scheduleClosed   = Color(0xFFE24B4A);


  // == Button == //

  static const destructiveBg      = Color.fromARGB(255, 198, 40,  40);
  static const destructiveBorder  = Color.fromARGB(255, 198, 40,  40);





}